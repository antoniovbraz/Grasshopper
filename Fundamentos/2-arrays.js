// Respostas mágicas
// Neste desafio, você editará uma array para salvar uma lista de palavras usando código.

// Adicione à array para dar à função pickRandom() mais palavras para escolher.

// INSTRUÇÕES
// Adicione outra string à array

let answer = ['yes', 'no', 'maybe']
let random = answer[Math.floor(Math.random() * answer.length)]
console.log(random)

// Faça um bolo
// Neste desafio, você usará uma array para salvar uma lista de ingredientes necessários para fazer um bolo.

// INSTRUÇÕES
// Substitua 'flour' por uma array[], por exemplo ingredients = []
// Adicione algumas novas strings como 'flour', 'sugar', 'eggs' na array de ingredientes
// let ingredients = 'flour';
//   console.log(ingredients);

let ingredients = ['flour', 'sugar', 'eggs']
console.log(ingredients)

// De quem são os sapatos?
// Qual variável está armazenando uma array? imprima no console.

let myShoes = 'sneaker, highHeels, pumps'
let yourShoes = ['sneaker', 'highHeels', 'pumps']
let shoeColor = 'Blue'

console.log(yourShoes)

// Como é possível atribuir os valores de novo de bananas e numberBananas para imprimir \"Let's make banana bread\"?

let bananas
let numberBananas
if (bananas !== 'green') {
  if (numberBananas > 5) {
    console.log("Let's make banana bread")
  }
}

// bananas = 'green'
// numberBananas = 10

bananas = 'yellow'
numberBananas = 6

if (bananas !== 'green') {
  if (numberBananas > 5) {
    console.log("Let's make banana bread")
  }
}

// bananas = 'brown'
// numberBananas = 3
